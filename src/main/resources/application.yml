server:
  port: 8080

spring:
  application:
    name: SentinelStream

  # Java 21 ??????? (2026 ????)
  threads:
    virtual:
      enabled: true

  # ???????
  datasource:
    url: jdbc:postgresql://localhost:5432/sentinel_stream
    username: admin
    password: password123
    driver-class-name: org.postgresql.Driver

  # JPA / Hibernate ??
  jpa:
    hibernate:
      ddl-auto: update # ???? Java ???????
    show-sql: true # ?????? SQL?????
    properties:
      hibernate:
        format_sql: true

# ????
logging:
  level:
    com.sentinel: DEBUG

